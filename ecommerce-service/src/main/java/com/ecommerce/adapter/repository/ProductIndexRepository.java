package com.ecommerce.adapter.repository;


import com.ecommerce.infrastructure.search.ProductIndex;

import org.springframework.stereotype.Repository;

import java.math.BigDecimal;
import java.util.List;
import java.util.UUID;

@Repository
public interface ProductIndexRepository{
/*
    List<ProductIndex> findByNomeContainingIgnoreCase(String nome);
    List<ProductIndex> findByCategoria(String categoria);
    List<ProductIndex> findByPrecoBetween(BigDecimal min, BigDecimal max);

 */
}

